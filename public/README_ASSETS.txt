Put TactLink brand assets here if needed:

- public/logo.svg
- public/og-image.png
- public/app-mockup.png

This starter currently uses CSS gradients and a custom mockup, so it can run without image assets.
